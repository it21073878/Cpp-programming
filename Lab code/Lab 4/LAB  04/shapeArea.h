struct cir {
	float rad;
};

struct rec {
	float length, width;
};

struct sqr {
	float length;
};


float areaofCircle(cir C);
float areaofRectangle(rec R);
float areaofSquare(sqr S); 
